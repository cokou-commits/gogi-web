# 五疑戦 UI FIX — 2026-09-11

ユーザー実機スクリーンショットから確認した表示問題だけを修正。ゲームルール・得点・カード効果・通信処理は変更していない。

- ルール画面の見出し左に出ていた縦線/装飾を完全削除
- ルールモーダルをモバイルでほぼ全幅化し、本文の有効横幅と余白を改善
- 通常/特殊カードに残っていた旧レスポンシブ `max-height` 制約を最終上書きし、544×928のカード画像を全表示
- `object-fit: contain` に固定し、カード下部の効果欄・枠が切れないよう修正
- HUDの秘密目標にタイトルだけでなく説明文も表示
- 上部HUDとチャット枠の間に4pxの明確な間隔を追加
- 760px以下の低い画面ではカードを縮めて切るのではなく、操作パネル側を内部スクロール可能に変更


## 2026-09-11 提供カード10枚を正式素材へ固定
- ユーザー提供画像を 攻撃 / 防御 / 偵察 / 告発 / 回復 / 完全防御 / 無効 / カード指定 / ポイント泥棒 / 2倍 に対応させて差し替え。
- 表示用に 544×928 へ統一し、切り抜かず全面表示。
- 長期キャッシュ対策として `ASSET_REV=20260911cardsExact` に更新。

## 2026-09-11 ターン解決エラー修正
- 会話・行動選択を統合した現行UIでは、ターン解決時の `room.phase` は `chat` のまま。
- 旧仕様の `resolveTurn()` が `action` フェーズのみ許可していたため、第1ターンから例外となり「全行動無効」のフェイルセーフへ落ちていた。
- `chat` を正式な解決元として許可し、内部検証用の旧 `action` も後方互換で許可。
- `result` 等からの二重解決は引き続き拒否。
- 回帰テスト2件を追加。
